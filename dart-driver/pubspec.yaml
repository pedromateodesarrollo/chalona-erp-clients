name: dart_driver_poc
description: Cliente HTTP para ecf-service (motor estático, sin dart_eval).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
